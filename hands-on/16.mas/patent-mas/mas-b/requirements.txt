# LangGraph: StateGraph-based Self-RAG workflow orchestration
langgraph>=0.2.0

# LangChain core framework (Runnable, prompts, output parsers, Document)
langchain-core>=0.3.0

# Groq LPU chat model integration (ChatGroq, openai/gpt-oss-120b, reasoning_format='hidden')
langchain-groq>=0.2.0

# OpenAI integration (query embeddings only: text-embedding-3-small for YouTube transcript chunk selection)
langchain-openai>=0.2.0

# LangChain community tools/utilities (DuckDuckGoSearchAPIWrapper)
langchain-community>=0.3.0

# DuckDuckGo web search backend (legacy package name, still imported by wrapper)
duckduckgo-search>=6.0.0

# DuckDuckGo search engine (new package; required after duckduckgo-search rename)
ddgs>=6.0.0

# Google API client for YouTube Data API v3 (date-filtered video search)
google-api-python-client>=2.100.0

# Transcript fetch backend used by LangChain YoutubeLoader (no proxy; graceful skip when unavailable)
youtube-transcript-api>=1.0.0

# Model Context Protocol SDK (Streamable HTTP client for the remote korean-law MCP server)
mcp>=1.2.0

# NumPy for in-memory cosine similarity (query-to-transcript-chunk relevance selection, no persistent vector store)
numpy>=1.26.0

# Structured output schemas for routing / reflection tokens (Self-RAG)
pydantic>=2.0.0

# Environment variable management (.env file support)
python-dotenv>=1.0.0
