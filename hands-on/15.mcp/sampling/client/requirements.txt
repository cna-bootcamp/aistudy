# MCP Sampling 예제 (서버·클라이언트 공통 의존성)
# 클라이언트가 sys.executable로 서버를 띄우므로 하나의 venv로 양쪽을 모두 실행함
mcp[cli]>=1.9.0       # MCP Python SDK (FastMCP 서버 + ClientSession)
openai>=1.0           # Groq OpenAI 호환 API 호출 (AsyncOpenAI) - 클라이언트
httpx>=0.27           # Slack Incoming Webhook 비동기 POST - 서버
python-dotenv>=1.0    # hands-on/.env 로드
