# 조건부 분기 (Splitting with Conditionals)

분기는 IF 또는 Switch node를 사용함. 단일 브랜치 workflow를 다중 브랜치 workflow로 전환함.
이는 n8n에서 복잡한 로직을 표현하는 핵심 요소임

## Workflow 비교

분기와 조건부 node의 활용을 통해 workflow의 실행 경로를 다양화할 수 있음

## 사용법

자세한 사용법은 IF 또는 Switch node 문서 참조
