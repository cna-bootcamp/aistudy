
[목표]
{예제경로}: start-here/hands-on/llm
{예제경로} 하위의 예제를 Google Colab에서 실습할 수 있는 버전으로 개발 

[역할]
- 리딩: 최아이 
- 비판: 클로니
- 협업: 다른 팀원  
[입력]
- 예제: {예제경로}
- 학습과정  
	- 모델 학습 방법 이해 
	- 인코딩 과정 이해
	   - 입력 문장을 토큰화, 임베딩, 위치 인코딩
	   - Multi-Head Attention:  복수의 Self-Attention을 서로 다른 관점에서 동시에 수행하여 입력 문맥을 이해하는 원리
	   - 잔차+정규화와 Feed Forward Network으로 더 풍성하게 맥락을 이해하는 원리
	- 디코딩 과정 이해
	   - 순차 응답 생성: Masked Self-Attention과 Cross-Attention의 원리
	   - 잔차+정규화와 Feed Forward Network으로 더 풍성하게 응답을 생성하는 원리
- 샘플매핑 
<학습과정과 샘플 매핑>
## 구성별 매칭
| 구성 | 매칭 샘플앱 | 실습 방식 |
|---|---|---|
| **모델 학습 방법 이해** | `train.py` + 웹 `#training` | 실행: epoch별 loss 출력(400 epoch). 웹: loss 곡선 재생(로그 스케일 전환 가능) |
| **인코딩 - 토큰화·임베딩·위치 인코딩** | `attention_demo.py`(토큰화·임베딩 부분) + `transformer_lab/tokenizer.py`, `positional_encoding.py` | 토큰화·임베딩은 `attention_demo.py` 실행 출력으로 확인 가능. **위치 인코딩은 전용 데모 없어 코드 리딩으로 설명 필요** |
| **인코딩 - Multi-Head Attention** | `attention_demo.py` + 웹 `#attention` | 실행: QK^T→스케일링→softmax→가중합 4단계 + 4개 헤드 결과 출력. 웹: 막대그래프 단계별/자동재생 |
| **인코딩 - 잔차+정규화+FFN** | `transformer_lab/layers.py`(`EncoderLayer`), `feed_forward.py` | **전용 실행 데모 없음 → 코드 리딩(`norm1(x+attn)` → FFN → `norm2(x+ffn)` 구조)으로 설명 필요** |
| **디코딩 - 순차 응답 생성(Masked Self-Attention+Cross-Attention)** | `translate.py` + 웹 `#translate` | 실행: `python translate.py --sentence "..."` 로 단어별 생성 확인. 웹: 생성 과정 애니메이션 + Cross-Attention 히트맵 |
| **디코딩 - 잔차+정규화+FFN** | `transformer_lab/layers.py`(`DecoderLayer`) | **전용 실행 데모 없음 → 코드 리딩(`norm1`→Cross-Attn→`norm2`→FFN→`norm3` 구조)으로 설명 필요** |
</학습과정과 샘플 매핑>

[처리]
- 예제 파악 및 이해: 최아이  
- 학습과정과 샘플매핑 이해: 모든 팀원 
- Colab 버전으로 재배치 및 개발 계획 수립: 최아이 
- 개발 계획 리뷰: 클로니 제외 다른 팀원이 병렬 수행  
- 개발 계획서에 리뷰 반영: 최아이 
- 개발 계획서 승인 요청: 클로니  
- colab 버전으로 재배치 및 개발: 최아이  
- 개발 결과 리뷰: 클로니 제외 다른 팀원이 병렬 수행  
- 개발 결과 비판: 노트가 수행 
- 리뷰와 비판 반영하여 최종 개발 완료 
- README.md 작성 
  - Colab 환경 설정: 노트가 수행. 공식문서 서치하여 작성  
  - 학습과정별 실습 방법: 최아이가 작성  

[출력]
colab 버전: start-here/hands-on/voc-translate/ (소스, README.md, requirements.txt)

[제약조건]
- MUST: 
  - 추가정보나 의사결정이 필요하면 반드시 요청 
  - context7 MCP 사용 
- MUST NOT 
  - 추측하여 생성하지말고 예제와 사실에 입각하여 작성 
- 완료조건 
  - 모든 출력물 생성  

